archivesBaseName = 'uuid-auth-client'

loom {
    runs {
        client {
            client()
            runDir = "run/client"
            ideConfigGenerated = true
        }
    }
}

dependencies {
}